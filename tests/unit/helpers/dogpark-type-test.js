
import { dogparkType } from 'dog-park/helpers/dogpark-type';
import { module, test } from 'qunit';

module('Unit | Helper | dogpark type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dogparkType([42]);
  assert.ok(result);
});

